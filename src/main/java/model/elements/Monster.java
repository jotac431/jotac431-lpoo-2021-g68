package model.elements;

import viewer.ElementViewer;

public class Monster extends Element {
    public Monster(int x, int y, ElementViewer.DIRECTION direction) {
        super(x, y, direction);
    }
}